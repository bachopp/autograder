var React = require("react");

// react-bootstrap

var Row = require("react-bootstrap").Row;
var Col = require("react-bootstrap").Col;

// local

var StudentResultSide = React.createClass({

    render: function() {

      return (
        <b>SIDE</b>
      );
    }
});

module.exports = StudentResultSide;
